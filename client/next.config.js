/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['major-project.com', 'st.depositphotos.com', 'rukminim2.flixcart.com'],
  }
}

module.exports = nextConfig
